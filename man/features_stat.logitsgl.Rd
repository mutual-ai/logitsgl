% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/navigate.R
\name{features_stat.logitsgl}
\alias{features_stat.logitsgl}
\title{Extract feature statistics}
\usage{
\method{features_stat}{logitsgl}(object, ...)
}
\arguments{
\item{object}{a logitsgl object}

\item{...}{ignored}
}
\value{
a vector of length \code{nmod(x)} or a matrix containing the number of nonzero features (or group) of the models.
}
\description{
Extracts the number of nonzero features (or group) in each model.
}
\examples{
data(birds)

# Simple standardization
X <- scale(X)

lambda <- logitsgl.lambda(X, Y, alpha = 0.5, lambda.min = 0.9)
fit <- logitsgl(X, Y, alpha = 0.5, lambda = lambda)

# Number of features in models
features_stat(fit)

fit.cv <- logitsgl.cv(X, Y, alpha = 0.5, fold = 10, lambda = lambda)

# Number of features
features_stat(fit.cv)
}
\author{
Martin Vincent
}

